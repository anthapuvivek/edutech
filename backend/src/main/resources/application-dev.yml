spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/learntrix}
    username: ${DB_USERNAME:postgres}
    password: "${DB_PASSWORD:postgres}"
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

app:
  jwt:
    secret: dev-secret-key-change-in-production-min-256-bits-long-string-here
    access-token-expiration: 900000
    refresh-token-expiration: 604800000
    remember-me-expiration: 2592000000

logging:
  level:
    com.learntrix: DEBUG

# Redis is optional for the current local backend scaffold. Keep the database
# health signal authoritative until Redis-backed features are introduced.
management:
  health:
    redis:
      enabled: false
