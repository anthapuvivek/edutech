spring:
  datasource:
    url: jdbc:tc:postgresql:16:///edtech_test
  flyway:
    enabled: true
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers:localhost:9092}

app:
  jwt:
    secret: test-secret-key-for-testing-only-min-256-bits

logging:
  level:
    root: WARN
    com.learntrix: INFO
