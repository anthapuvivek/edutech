$kind: http-request
name: Update Role Permissions
url: '{{baseUrl}}/api/rbac/roles/{{role}}'
method: PATCH
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
body:
  type: json
  content: |-
    {
      "permissions": []
    }
