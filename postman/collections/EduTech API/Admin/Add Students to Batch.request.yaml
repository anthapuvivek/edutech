$kind: http-request
name: Add Students to Batch
url: '{{baseUrl}}/api/admin/batches/{{batchId}}/students'
method: POST
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
body:
  type: json
  content: |-
    {
      "studentIds": []
    }
