$kind: http-request
name: Create Batch
url: '{{baseUrl}}/api/admin/batches'
method: POST
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
body:
  type: json
  content: |-
    {
      "name": "",
      "courseId": "",
      "startDate": "",
      "endDate": ""
    }
