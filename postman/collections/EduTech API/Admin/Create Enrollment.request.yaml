$kind: http-request
name: Create Enrollment
url: '{{baseUrl}}/api/admin/enrollments'
method: POST
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
body:
  type: json
  content: |-
    {
      "studentId": "",
      "courseId": ""
    }
