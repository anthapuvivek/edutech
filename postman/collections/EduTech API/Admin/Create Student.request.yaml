$kind: http-request
name: Create Student
url: '{{baseUrl}}/api/admin/students'
method: POST
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
body:
  type: json
  content: |-
    {
      "name": "",
      "email": "",
      "password": ""
    }
