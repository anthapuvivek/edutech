$kind: http-request
name: Mark Notification Read
url: '{{baseUrl}}/api/student/notifications/{{id}}/read'
method: POST
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
