$kind: http-request
name: Get Recordings by Course
url: '{{baseUrl}}/api/student/recordings/course/{{courseId}}'
method: GET
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
