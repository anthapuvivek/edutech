$kind: http-request
name: Create Note
url: '{{baseUrl}}/api/mentor/notes'
method: POST
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
body:
  type: json
  content: |-
    {
      "studentId": "",
      "content": ""
    }
