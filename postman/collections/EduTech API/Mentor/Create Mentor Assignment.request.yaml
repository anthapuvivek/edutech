$kind: http-request
name: Create Mentor Assignment
url: '{{baseUrl}}/api/admin/mentor-assignments'
method: POST
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
body:
  type: json
  content: |-
    {
      "mentorId": "",
      "studentId": ""
    }
