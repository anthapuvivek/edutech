$kind: http-request
name: Admin Get Eligibility Rules
url: '{{baseUrl}}/api/admin/career/eligibility-rules'
method: GET
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
