$kind: http-request
name: Update Offer Status
url: '{{baseUrl}}/api/placement/offers/{{id}}/status'
method: PATCH
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
body:
  type: json
  content: |-
    {
      "status": ""
    }
