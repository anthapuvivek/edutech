$kind: http-request
name: Update Application Status
url: '{{baseUrl}}/api/placement/applications/status'
method: PATCH
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
body:
  type: json
  content: |-
    {
      "applicationId": "",
      "status": ""
    }
