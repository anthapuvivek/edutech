$kind: http-request
name: Apply to Drive
url: '{{baseUrl}}/api/student/placement-drives/{{driveId}}/apply'
method: POST
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
